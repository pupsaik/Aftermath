<UserControl x:Class="Aftermath.Views.StartView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Aftermath.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="Black">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Foreground="Orange" Style="{StaticResource Title}" Text="AFTERMATH" FontSize="320"/>
            <Button Style="{StaticResource TitleButton}" Content="Normal" Command="{Binding NormalCommand}"/>
            <Button Style="{StaticResource TitleButton}" Content="Hard" Margin="0 20 0 0" Command="{Binding HardCommand}"/>
            <Button Style="{StaticResource TitleButton}" Content="Exit" Margin="0 20 0 0" Command="{Binding CloseCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
